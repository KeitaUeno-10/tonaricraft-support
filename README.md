# tonaricraft-support

Static site for tonaricraft.com.

## Deploy

```sh
npm run deploy
```

This deploys the HTML files to the existing Cloudflare Worker `tonaricraft-support`.
Use `npm run deploy:dry-run` to validate the upload before deploying.
